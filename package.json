{
  "name": "trabalho-ecommercepet-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c green,cyan \"npm run dev --prefix api_ecommercePet\" \"wait-on tcp:3001 && npm start --prefix front_react_ecommerce\"",
    "install:all": "npm install --prefix api_ecommercePet && npm install --prefix front_react_ecommerce"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "wait-on": "^7.0.1"
  }
}
